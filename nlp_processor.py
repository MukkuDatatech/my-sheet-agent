import spacy\nfrom collections import Counter\n\nclass NLPProcessor:\n    def __init__(self):\n        self.nlp = spacy.load('en_core_web_sm')\n\n    def text_cleaning(self, text):\n        # Basic text cleaning\n        return ' '.join(text.split())\n\n    def entity_extraction(self, text):\n        doc = self.nlp(text)\n        return [(ent.text, ent.label_) for ent in doc.ents]\n\n    def key_phrase_extraction(self, text):\n        doc = self.nlp(text)\n        return [chunk.text for chunk in doc.noun_chunks]\n\n    def lead_source_classification(self, text):\n        # Placeholder for lead source classification logic\n        return 'classified lead source'\n\n    def sentiment_analysis(self, text):\n        # Placeholder for sentiment analysis logic\n        return 'positive'  # This is just a placeholder\n\n    def intent_extraction(self, text):\n        # Placeholder for intent extraction logic\n        return 'extracted intent'\n\n    def message_summarization(self, text):\n        # Placeholder for message summarization logic\n        return 'summarized message'\n\n# Example usage\nif __name__ == '__main__':\n    processor = NLPProcessor()\n    sample_text = 'Your sample text goes here.'\n    print(processor.entity_extraction(sample_text))\n    print(processor.key_phrase_extraction(sample_text))